{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "concurrently 'npm run server' 'npm run nuxt dev'",
    "server": "node server/fastify-sqlite.cjs",
    "nuxt": "nuxt",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "axios": "^1.8.4",
    "fastify": "^5.3.0",
    "fastify-cors": "^6.1.0",
    "normalize.css": "^8.0.1",
    "nuxt": "^3.16.2",
    "sqlite3": "^5.1.7",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
